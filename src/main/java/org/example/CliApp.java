package org.example;

public class CliApp {




    public void printOptions(){
        /*
        User:
        1. Add new user (userName, password)
        2. Update password (userId, password)
        3. Delete user (userId)
        4. Get favorite movies (UserId)
        5. Add favorite movie (userId, movie)
        6. Remove favorite movie (userId, movie)
        7. Find users by username (userName)

        UserRating:
        1. Rate a movie (user, movie, rating)
        2. Get movies that you rated (user)
        3. Get movies by rating (min, max)
        4. Get your rating for a movie (user, movie)
        5. Get average rating for a movie
            **** CAN BE DELETED? DISPLAYED IN MOVIE ENTITY ****

        Movie:
        1. Add a new movie (title, date YYYY-MM-DD, length in minutes, country, language)
        2. Delete movie (id)
        3. List all movies
        4. List movies by language (language)
        5. List movies by ranking (min rank, max rank)
        6. List movies by length (min length, max length)
        7. List movies by release date (from, to)
        8. List movies by actor (actor)
        9. List movies by director (director)
        10. Find a movie by id (id)
        11. Find a movie by title (title)
        12. Add an actor to a movie (movieId, actorId) OR (movie, actor)
        13. Add a director to a movie (movieId, directorId)

        Genre:
        1. Add a new Genre (genreName) ***** ta bort? enum?
        2. Delete a genre (genreId)
        3. List all genres
        4. Find genre by Id (id) ***** Ej intressant?
        5. Find genre by name (name) ****** Ej intressant?


        Director:
        1. Add a new Director (directorName, country)
        2. Delete a director (id)
        3. List all directors from a country (country)
        4. Find director by ID (id)
        5. Find director by name (directorName)


        Actor:
        1. Add a new actor (actorname, country)
        2. Delete an actor (id)
        3. List actors by country (country)
        4. Find an actor (id) or (name)


         */
    }
}
